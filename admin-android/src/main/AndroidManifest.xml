<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.waw.messenger.permission.INTERNAL_ADMIN" />
    <application
        android:theme="@style/Theme.WAWControl"
        android:label="WAW Control Mobile"
        android:icon="@drawable/waw_control_logo"
        android:roundIcon="@drawable/waw_control_logo"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:permission="com.waw.messenger.permission.INTERNAL_ADMIN"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
