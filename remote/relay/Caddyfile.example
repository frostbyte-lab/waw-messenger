# Ganti relay.example.com dengan domain relay publik Anda.
relay.example.com {
    reverse_proxy relay:8787
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        X-Content-Type-Options "nosniff"
    }
}
